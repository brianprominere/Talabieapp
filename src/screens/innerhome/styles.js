const React = require("react-native");
const { Dimensions, Platform } = React;
const deviceHeight = Dimensions.get("window").height;

export default {
  cardview:{flex: 0,marginRight: 5, marginLeft: 0, marginTop: 0, marginBottom: 10,width: 345},
};
